export async function generateReportPdfUrl({ certificateNo }) {
  // v1: you can generate PDF in another service or later add pdfkit.
  // For now we return null and keep field optional.
  return null;
}